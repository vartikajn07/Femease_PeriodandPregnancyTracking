
const BASE_URL = 'https://period-pregnancy-tracker-app.onrender.com/'

const endpoints = {
    // Auth endpoints
    LOGIN: `${BASE_URL}api/auth/login`,
    REGISTER: `${BASE_URL}api/auth/register`
    
   
  
    // Add more endpoints here
  };
  
  export default endpoints;